# Copy this file to .env and set a strong password before starting the container.
# The .env file is gitignored and must NEVER be committed.
#
#   cp .env.example .env
#   nano .env   # replace the placeholder with a strong password

REDIS_PASSWORD=change_me_strong_password
